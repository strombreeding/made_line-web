import CombinedGraph from "./client";
import DonutChart from "./donut";

export default async function Page() {
  // Next API로

  return (
    <div>
      <CombinedGraph />;
      <DonutChart />
    </div>
  );
}
